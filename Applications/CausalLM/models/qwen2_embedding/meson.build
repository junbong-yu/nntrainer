qwen2_embedding_src = [
    meson.current_source_dir() / 'qwen2_embedding.cpp',
]

qwen2_embedding_inc = include_directories('.')

causallm_src += qwen2_embedding_src
causallm_inc += qwen2_embedding_inc
